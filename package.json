{
  "name": "server",
  "version": "0.1.0",
  "main": "index.js",
  "dependencies": {
    "cookie-parser": "1.3.2",
    "dotenv": "^8.2.0",
    "express": "^4.15.3",
    "path": "^0.12.7",
    "querystring": "~0.2.0",
    "request": "~2.83.0"
  },
  "engines": {
    "node": "10.13.0"
  },
  "scripts": {
    "start": "node ./Spotify/Login.js",
    "install": "cd client && npm install",
    "server": "node ./Spotify/Login.js",
    "client": "cd client && npm start",
    "build": "react-scripts builds",
    "heroku-postbuild": "cd client && npm install && npm run build"
  }
}
