{
  "name": "server",
  "version": "0.1.0",
  "main": "index.js",
  "engines": {
    "node": "14.x"
  },
  "dependencies": {
    "cookie-parser": "1.3.2",
    "dotenv": "^8.2.0",
    "express": "^4.15.3",
    "path": "^0.12.7",
    "querystring": "~0.2.0",
    "request": "~2.83.0"
  },
  "scripts": {
    "start": "concurrently \"npm run watch --prefix server\" \"npm start --prefix client\"",
    "install": "cd client && npm install",
    "server": "node ./Spotify/Login.js && npm install",
    "client": "cd client && npm start",
    "heroku-postbuild": "cd client && npm install && npm run build"
  }
}
